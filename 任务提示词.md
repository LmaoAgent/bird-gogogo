# 《蒜鸟冲冲冲》· 任务提示词批次(2026-07-18)

用法:每条任务的代码块内是**自包含提示词**,整段复制给新 session 即可;也可以在新 session 里说"读 `任务提示词.md` 执行 T3"。

**主控与验收**:本批次由主控 session 统一验收——每个任务做完把**改动文件清单 + 自测结论**报给主控,主控按各任务"验收"清单核收。多任务并行时以依赖表的**文件边界**为准,不抢跑、不越界改别人的文件。

**总纲**:PRD = `PRD.md`(v1.0,决策已全部回填);玩法真源 = `玩法数值与关卡设计.md`;美术 spec = `美术需求清单.md`;变现/社交 = `广告接入spec.md`/`排行榜接入spec.md`/`签到任务分享系统设计.md`。**已交付美术** = `art_delivery/p0/`(全套 P0 资源 + 3 套图集 + 接入清单)。**可跑原型** = `prototype/`(纯 JS+Canvas,核心闭环已通)。

## 推荐顺序与依赖

| 编号 | 任务 | 类型 | 前置 | 主要改动文件 | 并行性 |
|---|---|---|---|---|---|
| T1 | 关卡扩充 11–40 关 + 难度曲线延展 | 配置/数值 | 无 | `prototype/config/levels.json` | **立即可派**,与 T2/T3 并行 |
| T2 | 星级公式修正(主控已拍板)+ 文档同步 | core 小改 | 无 | `prototype/src/core/{rules,game}.js`、`config/tuning.json`、`玩法数值与关卡设计.md` | **立即可派**,与 T1/T3 并行 |
| T3 | 养成/金币/存档纯逻辑层 | core 新增 | 无 | `prototype/src/core/progress.js`(新)、`config/upgrade.json`(新) | **立即可派**,与 T1/T2 并行 |
| T4 | Cocos 工程搭建 + core 迁入 + 闭环跑通 | 工程大改 | 装 Cocos(✅3.8.8 已装) + **T2/T3 交付**(第 2 步迁的就是它们的成果,早派会迁到旧版 core) | `cocos/`(新工程) | B 组地基,T5~T7 前置 |
| T5 | 美术接入:图集/九宫格/动画/位图字体 | 前端大改 | T4 | `cocos/assets/` | 与 T6 错峰(同工程) |
| T6 | UI:主界面/HUD/结算/失败页 | 前端中改 | T4 | `cocos/assets/scripts/ui/` | 与 T5 错峰 |
| T7 | 特效与手感打磨(吃门/撞击/震屏/数字滚动) | 前端中改 | T5 | `cocos/assets/scripts/fx/` | T5 后 |
| T8 | 微信小游戏构建 + 真机跑通 + 存档接线 | 平台适配 | T4~T6 | `cocos/`(构建配置) | C 组地基 |
| T9 | 广告接入(激励视频复活/翻倍) | 中改 | T8 | `cocos/assets/scripts/core/AdManager.ts` | 与 T10/T11 并行 |
| T10 | 排行榜 + 好友 PK(开放数据域) | 中改 | T8 | `cocos/openData/`、`scripts/ui/Rank*` | 与 T9/T11 并行 |
| T11 | 签到/每日任务/分享裂变 | 中改 | T8 | `cocos/assets/scripts/systems/` | 与 T9/T10 并行 |
| T12 | 上线准备:备案/类目/隐私/流量主/素材 | 部署运营 | T9 合入 | — | **主控自留,不派发** |

## 环境事实与并行纪律(每个开工 session 必读)

- **项目根** `/Users/mm/project/蒜鸟的战斗`(注意:目录名是旧名,产品正式名＝**蒜鸟冲冲冲**)。**已是 git 仓库**(2026-07-18 初始化,首提含文档包 + prototype + art_delivery)。**并行纪律**:严格按依赖表的**文件边界**改动;`git commit` **一律带路径限定**(`git commit <你改的文件> -m "..."`),**禁止 `git add -A`**——多 session 同时在 main 上干活,`-A` 会吃进别人的改动;提交后 `git show --stat HEAD` 核对一遍。远程 = `https://github.com/LmaoAgent/bird-gogogo.git`(origin/main)。**session 只 commit,不 push**——push 由主控验收后统一做(等价于参考流程里的「开 PR 不合并」)。
- **prototype/ 的定位(重要)**:它不是要被丢掉的草稿,而是**规则参考实现 + 数值验证台**。`src/core/`(`rules.js` 纯规则 + `game.js` 局内状态机)**不引用任何 DOM/Canvas**,迁 Cocos 时与 `config/` 原样搬走;`render.js`/`input.js`/`sprites.js` 是表现层,迁 Cocos 时整体替换。**T1/T2/T3 的成果(config + core)迁移后两边共用**,所以现在就做不算白做。
- **已实现的规则(读前先看,别重造)**:`rules.js` 有 `applyGateEffect`/`inLane`/`resolvePick`/`expandGates`(连排门展开)/`resolveSmash`(§4.2 结果只由 k 决定)/`smashDuration`(只影响观感)/`buildWaves`(BOSS 多阶段串联)/`starRating`/`unitFormationSlot`(黄金角圆盘布点);`game.js` 的 `Game` 类跑 running→smashing→win/fail 状态机,每帧产出 `events`(`gate`/`smashStart`/`smashEnd`)给表现层消费。
- **数值纪律**:数值一律在 `config/tuning.json`(手感)与 `config/levels.json`(关卡)里,**代码不写死**。tuning 字段:`startArmy/forwardSpeed/dragSensitivity/followSmooth/formationRadiusK/nRender/combat{dps,tMin,tMax}/fx{...}/track{width,laneX{left,center,right},gateHalfWidth}/star`。
- **原型怎么跑**:`.claude/launch.json` 已配好(`python3 -m http.server 8099`,根目录=`prototype/`);浏览器开 `http://localhost:8099/`,`?level=8` 直接进第 8 关。
- **⚠️ 验证方式(踩过的坑)**:页面暴露 `window.__proto`(`game`/`goto(lv)`/`tuning`/`levels`)。**自动化浏览器会节流 requestAnimationFrame**,靠 sleep 等推进游戏不可行——要在 console 里手动 `for(let i=0;i<600;i++) game.update(1/60)` 循环,并直接设 `game.targetX` 模拟走位。
- **美术已交付**:`art_delivery/p0/`——`assets/`(角色11/敌人8/BOSS5/场景10/门4/位图字体2套/特效16/UI16/品牌4)、`atlases/`(characters/world/ui 三套 PNG+JSON+PLIST)、`integration_manifest.json`(设计分辨率 1080×1920 @2x、动画帧率、九宫格 inset)、`交付说明.md`(含 Cocos 接入建议)、`scripts/validate_p0_assets.py`(验收脚本)。**美术接入直接在 Cocos 里做(T5),不要回头去改 prototype 的占位美术**(`sprites.js` 是程序绘制的占位,迁移后整体退役)。
- **技术路线(已拍板)**:迁 **Cocos Creator 3.8+ / TypeScript**,构建目标微信小游戏。工程结构照 `P0工程骨架.md` §1(core/game/ui/defs 分层),但**逻辑层不重写**——`core/` 从 prototype 原样搬。
- **红线(全批次适用)**:① `core/` 与 `config/` 保持**引擎无关**,不许把 Cocos 的 `cc.*` 或 DOM 混进去;② 数值不硬编码;③ 首包 ≤ 4MB(超了走分包/远程);④ 美术命名照 `美术需求清单.md` §0,不自创。

**老板人肉待办**(session 干不了):
1. ~~装 Cocos Creator 3.8+~~ ✅ **已完成**:**3.8.8** 在 `/Applications/Cocos/Creator/3.8.8/CocosCreator.app`(已验签、无 Gatekeeper 隔离,可直接打开)。
2. ~~`git init` + 首次提交~~ ✅ **已完成**(2026-07-18)。
3. 注册**微信小游戏账号 + AppID**(企业主体),T8 真机调试要用。
4. **「蒜鸟冲冲冲」名称查重**(微信后台,小游戏名唯一且需过审;撞名要备候选)。
5. 开通**流量主**(T9 广告要正式 adUnitId;联调可先用测试 ID)。

---

## T1 关卡扩充 11–40 关 + 难度曲线延展

```text
项目 /Users/mm/project/蒜鸟的战斗(不是 git 仓库,只改本任务文件:prototype/config/levels.json)。

背景:PRD 定首发 40 关,现在 prototype/config/levels.json 只有 10 关。玩法真源=《玩法数值与关卡设计.md》§3(门)/§4(对撞)/§5(关卡与曲线)。规则引擎在 prototype/src/core/rules.js,已实现 add/mul/sub/div/pick 五种门、repeat 连排展开、BOSS 多阶段(phases 数组串联成多段 H)。

现有格式(照抄结构,别改字段名):
{ "level":8, "note":"设计意图+验算", "trackLength":420, "k":3.5, "targetN":150,
  "gates":[ {"id":"l8_blue_a","type":"add","value":1,"side":"left","posZ":30,"repeat":{"count":30,"stepZ":4}},
            {"id":"l8_pick","type":"pick","posZ":220,"options":[{"type":"mul","value":2,"side":"left"},{"type":"add","value":80,"side":"right"}]} ],
  "enemies":[{"type":"moldling","H":400,"posZ":400}], "boss":null }
BOSS 关:"boss":{"type":"rotgarlic","phases":[250,250,250],"posZ":450},"enemies":[]。side 只能是 left/center/right(对应 tuning.track.laneX)。

任务:
1. 补 11–40 关。难度曲线:k 从 3.3 继续缓降到长期 2.0(别断崖);H 随关上升;trackLength 随之加长(现 10 关是 260→470,保持节奏别让单局超 90 秒)。
2. 节奏纪律:每 5 关一个小 BOSS(type=rotgarlic_elite,单段 phases)、每 20 关一个大 BOSS(rotgarlic,3 段)。即 15/25/35 关小 BOSS,20/40 关大 BOSS。
3. 门的花样按关递进组合(现有 10 关已教完全部五种门):双 pick 连续抉择、陷阱贴在黄门同 lane 后方逼走位、连排在 pick 之后放大乘法收益、÷2 与 ×2 对冲等。每关至少 3 个决策点。
4. **每关必须写 note**,格式照现有:一句设计意图 + **可验算的路线结论**(如"选×2 = 110 < N_min 115 必败,选+80 = 150 过")。targetN 填该关最优路线的兵力。
5. 核心校验(每关都要算):最优路线兵力 ≥ N_min(=总 H/k) 且留合理余量;**至少一条次优路线 < N_min**(制造"要吃对门才过");无操作(一路 center 不动)在 11 关之后应当过不了。

红线:只改 levels.json,不碰 core/ 与 render;不新增门 type(五种够用,新 type 要改引擎=越界);数值不写进代码。
验收(主控核):① 40 关齐、字段合法(JSON 能被 expandGates/buildWaves 直接消费);② 抽查 15/20/25/30/35/40 关,用 window.__proto 手动步进验算最优路线可过、给出的次优路线确实过不了;③ note 的验算数字与实际运行一致(主控会抽 3 关跑数);④ 单局时长不超 90 秒。
```

## T2 星级公式修正 + 文档同步

```text
项目 /Users/mm/project/蒜鸟的战斗。只改:prototype/src/core/rules.js、prototype/src/core/game.js、prototype/config/tuning.json、玩法数值与关卡设计.md(§6)。

背景(主控已拍板,照此实现,不要自己另设方案):现行星级 `rem = N_end / N_peak`(rules.js starRating)与难度曲线冲突——高关 k 下降使 N_min 吃掉绝大部分兵力,实测关 1 走满=0.9→3★,关 8 最优=0.238→1★,关 10 最优=0.127→1★,**玩得越好星级越低**,反馈负向。

改为按「离本关最优解有多近」评星:
  ratio = nPeak / level.targetN        // nPeak=本局峰值兵力,game.js 已在记录;targetN 每关配置已有
  ratio ≥ 0.95 → 3★;≥ 0.75 → 2★;通关即 1★;未通关 0★
验算(改完必须对上):关 1 走满 30/30=1.0→3★;关 8 最优 150/150=1.0→3★、走×2 路 110/150=0.73→1★;关 10 的 +80 路 259/260≈1.0→3★、×2 路 229/260=0.88→2★。

任务:
1. rules.js:`starRating(nPeak, targetN, thresholds)` —— 改签名与实现,保留"nEnd<=0 → 0★"的判负前置(判负由 game.js 决定,函数只管通关后的评级)。
2. game.js:`#win()` 调用处改为传 `this.nPeak` 与 `this.level.targetN`;`result` 里补 `ratio` 字段(便于结算页展示"最优度")。
3. tuning.json:`star` 阈值改 `{"3":0.95,"2":0.75}`;关卡级覆盖(level.star)语义保持不变。
4. 同步《玩法数值与关卡设计.md》§6:把星级定义改成新公式,并写明**为什么**(旧公式与 k 曲线冲突,一句话结论即可,别写长篇)。

红线:只动星级,不碰对撞模型(resolveSmash)与胜负判定;core/ 保持引擎无关(不引入 DOM);不改 levels.json(那是 T1 的文件)。
验收(主控核):① 上面 5 条验算全部对上(主控用 window.__proto 跑关 1/8/10 复核);② 未通关仍为 0★;③ 文档 §6 已同步且说明了原因;④ prototype 打开能正常玩到结算。
```

## T3 养成/金币/存档纯逻辑层

```text
项目 /Users/mm/project/蒜鸟的战斗。新增:prototype/src/core/progress.js、prototype/config/upgrade.json。不改现有文件(除非主控另行确认)。

背景:《玩法数值与关卡设计.md》与 PRD §5 定义了轻养成:起始兵力升级、单兵强度升级、金币(**首发单货币,无钻石**——PRD §5 已拍板)。当前 prototype 只有局内逻辑,没有局外成长与存档。本任务只做**引擎无关的纯逻辑层**,不做 UI、不碰渲染——Cocos/微信各自实现存储适配后复用它。

任务:
1. `config/upgrade.json`:两条升级曲线(起始兵力 N0、单兵强度)——每级 {level, value, cost}。约束:N0 从 tuning.startArmy=10 起步,升级要能明显缩短前期重复关卡的时间但不破坏关卡"要吃对门"的抉择(建议 N0 上限 ≈ 40,不足以让玩家跳过门就过关);金币成本随级数递增。
2. `core/progress.js`(纯函数 + 一个 Progress 类,**零 DOM / 零 Cocos API**):
   - 存档数据结构:`{ version, maxLevel, coins, upgrades:{startArmy, unitPower}, stars:{[level]:star} }`;
   - `applyLevelResult(progress, level, result)` → 通关写入最高关卡/星级(取更高)/结算金币;
   - 金币公式:按关卡与星级给(建议 base×level + star 奖励,写进 upgrade.json 不写死);
   - `canUpgrade/applyUpgrade(progress, kind)` → 校验金币并升级;
   - `getStartArmy(progress, tuning)` → 供 Game 构造时替代 tuning.startArmy(**只暴露纯函数,不去改 game.js**,接线由 T4 在 Cocos 侧做)。
   - **存储适配用注入**:`new Progress(storageAdapter)`,adapter 只需 `load():object|null` / `save(obj):void`;prototype 里给个 localStorage 实现放在**表现层**(src/,不是 core/)做冒烟。
3. 版本迁移:存档带 `version`,写一个 `migrate(raw)` 兜底(字段缺失给默认值),避免后续加字段炸档。

红线:core/progress.js **绝不引用 DOM/localStorage/Cocos**(存储走注入的 adapter);单货币(不许出现钻石/gem);不改 game.js/rules.js/levels.json/tuning.json(别人的文件)。
验收(主控核):① 在浏览器 console 里 new 一个内存 adapter 跑通:通关写档→金币累加→升级→getStartArmy 生效;② 升级曲线满级 N0 仍不足以"不吃门就过第 20 关"(主控会算);③ progress.js 全文 grep 不到 document/window/localStorage/cc.;④ 存档缺字段时 migrate 不炸。
```

## T4 Cocos 工程搭建 + core 迁入 + 闭环跑通

```text
项目 /Users/mm/project/蒜鸟的战斗。新建 Cocos 工程于 `cocos/`。前置:① 本机已装 **Cocos Creator 3.8.8**(`/Applications/Cocos/Creator/3.8.8/CocosCreator.app`,已验证可用);② **T2/T3 必须已交付**——本任务第 2 步迁入的就是它们改过的 core(星级公式/progress),未交付先停,报主控。

背景:技术路线已拍板——迁 Cocos Creator 3.8+/TypeScript,构建目标微信小游戏,竖屏 1080×1920。**逻辑层不重写**:prototype/src/core/(rules.js + game.js)与 prototype/config/(tuning.json + levels.json)是已验证的规则真源,**原样搬入**(可改为 .ts 但逻辑一行不动,类型按现有字段补)。工程分层照《P0工程骨架.md》§1(core/game/ui/defs),但本任务只搭到"能玩"为止。

任务:
1. 新建 Cocos 3.8 工程 `cocos/`,TS,设计分辨率 1080×1920 竖屏,适配 FIT_HEIGHT。
2. 迁入逻辑层:`assets/scripts/core/rules.ts`、`core/game.ts`(从 prototype 原样搬,补 TS 类型;**禁止改规则逻辑**)、`assets/resources/config/{tuning,levels}.json`(直接复制,后续两边同步以 prototype 为准)。同时迁入 T3 的 `core/progress.ts`。**⚠️ 必须接线,否则养成白做**:Game 构造时用 `getStartArmy(save, tuning, config)` 替代 `tuning.startArmy`;对撞演出时长用 `getUnitPower(...)` 替代 `tuning.combat.dps`(它只进 `smashDuration`,不进胜负判定);结算后调 `Progress.applyLevelResult(level, game.result)` 落盘。存储 adapter 照 `prototype/src/storage.js` 的 `{load,save}` 形状实现一份 Cocos/微信版(**adapter 属表现层,不进 core/**)。
3. 表现层最简实现(**先跑通,不做美术**——美术是 T5):用色块/纯色 Sprite 画大军(按 `unitFormationSlot` 布点,同屏上限 tuning.nRender)、门、敌人、BOSS;赛道用纯色 + 简单滚动。
4. 输入:触摸拖动 → `game.dragBy(deltaWorldX)`(注意屏幕像素→世界坐标换算走 tuning.dragSensitivity,行为对齐 prototype/src/input.js)。
5. 主循环:每帧 `game.update(dt)` → 消费 `game.events`(gate/smashStart/smashEnd)→ 更新表现;胜负后可点击重开/下一关。
6. 对象池:大军单位必须池化(几百只,禁止逐帧 new/destroy),池实现放 `core/ObjectPool.ts`。

红线:**core/ 与 config/ 保持引擎无关**——rules/game/progress 里不许出现 cc.*、Node、Component;规则逻辑零改动(改了=偏离已验证真源,主控会 diff);数值不硬编码。
验收(主控核):① Cocos 编辑器里能完整玩:出兵→拖动吃门→撞击判定→通关/失败→下一关;② **行为与 web 原型一致**——同一关、同样走位序列,结果兵力与胜负相同(主控会用 window.__proto 跑 prototype 对照,抽 3 关);③ core/*.ts grep 不到 cc.;④ 大军 200+ 时编辑器预览不卡(池化生效)。
```

## T5 美术接入:图集/九宫格/动画/位图字体

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程在 `cocos/`。前置:T4 已跑通。只改 cocos/assets/(与 T6 错峰,别动 scripts/ui/)。

背景:美术已全套交付在 `art_delivery/p0/`。**接入清单是 `art_delivery/p0/integration_manifest.json`(权威)**:设计分辨率 1080×1920、@2x、三套图集(`atlases/p0_characters_atlas`、`p0_world_atlas`、`p0_ui_atlas`,各有 PNG+JSON+PLIST)、`animations` 段给了每个动画的帧序列/pattern/fps/loop/blend、`nine_slice_insets_px_at_2x` 给了门框与 UI 的九宫格 inset。另有 `交付说明.md`(含 Cocos 接入建议)与 `scripts/validate_p0_assets.py`(验收脚本)。

任务:
1. 导入图集:优先用 `.plist` + PNG(Cocos 管线原生);**不要同时绑定 .json 与 .plist 两份元数据**(交付说明明确警告)。
2. 替换 T4 的色块为真实精灵:
   - 大军=`hero_garlicbird_run`(P0 是静态关键帧,loop=true;集群靠 tint/scale/相位微扰做丰富度,别逐只做资源);通关欢呼=`hero_garlicbird_cheer`,被消灭=`hero_garlicbird_panic`;
   - 杂兵=`enemy_moldling_flow`(4 帧 8fps 循环)、消灭=`enemy_moldling_die`(4 帧 12fps);
   - BOSS=`boss_rotgarlic_*`(idle/roar/hit/phase2/die 五姿态,按 game.js 的 wave.phase 切 phase2);
   - 场景=`scene_marketbridge_track_tile`(**可循环平铺**,随 game.z 滚动)+ `background`(远景)+ 6 个道具点缀;
   - 门=四类门框(蓝加成/金大额/紫乘法/红黑陷阱),**按 nine_slice_insets 设 Sliced**。
3. 门上数字用交付的**位图字体**:`font_gate_bonus_01`(加成绿白)/`font_gate_trap_01`(陷阱红)——.fnt+.png,加成门用 bonus、sub/div 用 trap;数字由程序渲染(值来自配置),不做成图。
4. 首包体积:场景大图用交付里的 `*_runtime_01.png`(已缩小版)进首包,原图走分包/远程;接完记录首包大小报主控。
5. 接完跑 `python3 art_delivery/p0/scripts/validate_p0_assets.py` 确认资源完整性(脚本已交付,别自己另写)。

红线:资源命名照《美术需求清单》§0,不自创、不重命名交付文件;不碰 scripts/ui/(T6 的地盘);不改 core/ 与 config/;美术只接不改(要改形象报主控,涉及版权口径)。
验收(主控核):① 与交付的 4 张竖屏基准稿(`assets/ui/ui_screen_home/hud/result_win/result_fail`)观感一致;② 门框九宫格拉伸不变形、门数字用对了两套字体;③ 赛道循环无缝、BOSS 进 phase2 有视觉变化;④ 大军 200+ 帧率稳(集群没退化成逐只资源);⑤ validate 脚本通过 + 首包大小数字。
```

## T6 UI:主界面/HUD/结算/失败页

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程 `cocos/`。前置:T4 已跑通(T5 未完成也可开工,先用占位图,接口对齐即可)。只改 cocos/assets/scripts/ui/ 与对应预制体(与 T5 错峰)。

背景:UI 资源已交付(`art_delivery/p0/assets/ui/`:按钮 start/restart/reward、通用弹窗、BOSS 血条、进度条、6 个图标(军队/金币/星/广告/暂停/关闭)、4 张竖屏基准稿),九宫格 inset 见 integration_manifest.json。数据来源:`game.n`(实时兵力)、`game.progress`(0~1 进度)、`game.currentWave`(血条:wave.H 与 smash.hAfter)、`game.result`({win,nEnd,nPeak,star,ratio})、T3 的 progress(金币/最高关/星级)。

任务:
1. 主界面:Logo(branding)、开始按钮、最高关卡与金币显示;皮肤/排行榜/签到入口先做**占位按钮**(P1 任务接,点了给"敬请期待")。
2. 关卡内 HUD:当前兵力数(大字,变化时跳动)、关卡进度条、**敌方/BOSS 血条**(BOSS 多阶段要显示"第 N/3 段")、暂停按钮。
3. 结算页(win):星级 1~3★(用 result.star)、本局峰值兵力、金币结算、"下一关"按钮;**广告翻倍按钮做出来但先禁用/占位**(T9 接 AdManager)。
4. 失败页(fail):失败原因提示(兵力被耗尽/撞不动第 N 波)、"重开"按钮;**看广告复活按钮占位**(T9 接)。
5. 与逻辑解耦:UI 只读 game 字段与消费 events,**不许把玩法逻辑写进 UI**(如别在 UI 里算星级/金币,调 core 的函数)。

红线:不碰 core/、config/、美术资源本体;金额/星级/金币一律用 core 返回值,UI 零计算;按钮触区 ≥ 88px@2x(竖屏手游标准)。
验收(主控核):① 完整一局 UI 流转:主界面→开始→HUD 实时正确(兵力/进度/血条)→通关结算(星级与 T2 公式一致)→下一关;→失败页→重开;② BOSS 三段血条表现正确;③ 广告按钮占位存在且不报错;④ grep 确认 ui/ 里没有星级/金币计算逻辑。
```

## T7 特效与手感打磨

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程 `cocos/`。前置:T5 美术已接入。改 cocos/assets/scripts/fx/ 与相关预制体。

背景:特效资源已交付——`fx_gateflash_play_*`(8 帧 16fps,**additive 混合**)、`fx_impact_garlic_*`(4 帧 16fps,蒜末)、`fx_impact_mold_*`(4 帧 16fps,霉菌爆开);参数见 integration_manifest.json 的 animations 段。手感参数在 config/tuning.json 的 `fx`:`smashShakeAmp:8`、`smashShakeDuration:0.2`、`bossHitStop:0.06`。对撞演出时长由 `smashDuration()` 给出(§4.2:只影响观感不改结果),`smash.progress` 是 0~1 演出进度。

任务:
1. 吃门光效:消费 `events` 里 kind='gate' 事件,在门位置播 gateflash(additive);加成门与陷阱门用不同色调区分(加成偏亮/陷阱偏红)。
2. 撞击演出:kind='smashStart' 起播——按 `smash.progress` **数字滚动**(我方兵力 nBefore→nAfter、敌方血量 hBefore→hAfter,两边同步下降,这是本品类的爽点)、蒜末与霉菌爆开特效交替、震屏(用 tuning.fx.smashShakeAmp/Duration)。
3. BOSS 受击顿帧:`bossHitStop` 秒的 time scale 停顿(只停表现,**不许停 game.update 的逻辑推进**,否则会改变判定时序)。
4. 兵力变化反馈:加兵时数字跳动 + 队形扩散动画(队形半径按 R=formationRadiusK·√N 平滑过渡,别瞬移);减兵/陷阱给红色闪烁。
5. 手感调参:把 §8 手感参数表(forwardSpeed/dragSensitivity/followSmooth/formationRadiusK)在真机/预览里过一遍,**调整值写回 config/tuning.json 并在 PR 说明里记录前后对比**。

红线:特效不改结果(对撞胜负只由 resolveSmash 决定);顿帧只作用于表现层;不碰 core/;新增参数一律进 tuning.json 不写死。
验收(主控核):① 吃门/撞击/BOSS 顿帧/震屏全部有反馈且不遮挡关键信息(兵力数、血条);② 数字滚动与 smash.progress 同步、结束值与 core 结算值一致(不许出现"演出显示过了但判负");③ 队形扩散平滑无瞬移;④ 连续玩 5 关不掉帧;⑤ tuning 调参记录。
```

## T8 微信小游戏构建 + 真机跑通 + 存档接线

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程 `cocos/`。前置:T4~T6 完成(T7 可并行)。需要微信小游戏 AppID(人肉待办③)与微信开发者工具。

背景:PRD §9/§10——平台微信小游戏,竖屏,**首包 ≤ 4MB**,先做纯广告版(无内购)。存档:T3 的 `core/progress.ts` 是引擎无关纯逻辑,存储走注入的 adapter,本任务实现微信版 adapter。

任务:
1. Cocos 构建微信小游戏:构建配置(AppID、竖屏、设计分辨率)、微信开发者工具能打开并运行、**真机预览跑通一局**。
2. 首包瘦身:资源分包(scene 大图、后续关卡资源走分包或远程 CDN),记录首包大小;超 4MB 必须处理到达标。
3. 存档适配:实现 `WxStorageAdapter`(`wx.setStorageSync/getStorageSync`)注入 Progress;加**微信云备份**预留(先本地,云备份留接口)。
4. 生命周期:`wx.onShow/onHide` 处理暂停/恢复(切后台不能继续跑 game.update 导致"回来就死了");`wx.onError` 兜底上报。
5. 埋点桩:实现 `Analytics.track(evt, params)`(先 console + 缓冲),按《玩法数值与关卡设计.md》§10 接玩法事件(level_start/gate_pick/wave_smash/level_win/level_fail)。

红线:不改 core/ 与 config/;不引入内购相关 API(纯 IAA,版号未到);切后台必须暂停逻辑;首包硬指标 ≤4MB。
验收(主控核):① 微信开发者工具 + **真机**各跑通一局完整流程;② 首包大小数字达标;③ 杀进程重进存档还在(关卡进度/金币/星级);④ 切后台再回来游戏状态正确(不掉兵、不误判负);⑤ 埋点事件在 console 可见且字段齐。
```

## T9 广告接入(激励视频复活/翻倍)

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程 `cocos/`。前置:T8 真机跑通。实现 cocos/assets/scripts/core/AdManager.ts + 结算/失败页接线。

背景:**实现规范见 `广告接入spec.md`(权威,照它做)**——P0 只接两个广告位:`revive`(FAIL 后复活续冲)与 `double`(通关结算金币翻倍);`boost`/`freebox`/`inter_level` 本任务不做(P1 补)。复活规则见《玩法数值与关卡设计.md》§7:复活后兵力 = max(当前波 N_min, N_peak×0.5),每局上限 2 次。

任务:
1. `AdManager.ts`:按 spec §3 的接口设计(`init(unitMap)`/`preloadRewarded()`/`canShow(scene)`/`showRewarded(scene)`),**实例全局复用**(不要每次 new)。
2. 关键坑(spec §3.1,必须照做):**只认 `onClose` 的 `res.isEnded===true` 才发奖**;show 失败先 `load()` 再 show;加载失败/无填充时 `canShow=false` → 按钮置灰或隐藏(不能点了没反应);show 后重新 load 备下一次。
3. 接线:失败页"看广告复活"→ showRewarded('revive') → ended 才复活续冲(复活兵力按 §7 公式,调 core 不要在 UI 里算);结算页"金币翻倍"→ showRewarded('double') → ended 才 ×2 入账。
4. 频控:revive 每局 ≤2 次(超了按钮不出),double 每次通关 1 次机会;频控参数进配置不写死。
5. 埋点:按 spec §6 接 `ad_request/ad_show/ad_close/ad_reward/ad_error`(带 scene 字段)。
6. 联调用**测试 adUnitId**;正式 ID 待流量主开通后替换(留配置位,别硬编码)。

红线:未看完不发奖(这是收入合规底线,也防白嫖);复活兵力用 core 公式;不接内购;不做 banner。
验收(主控核):① 真机看完广告→复活生效/金币翻倍;② **中途退出广告→不发奖**(主控会实测这条);③ 无广告填充时按钮置灰不报错;④ revive 每局 2 次上限生效;⑤ 埋点五个事件齐、scene 字段正确。
```

## T10 排行榜 + 好友 PK(开放数据域)

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程 `cocos/`。前置:T8。新增 cocos/openData/(子域)+ scripts/ui/Rank*。

背景:**实现规范见 `排行榜接入spec.md`(权威)**。关键机制:微信好友数据**只能在开放数据域子域**读取与绘制,主域拿不到——主域 `wx.getOpenDataContext().postMessage()` 发指令,子域 `wx.onMessage` 接;子域只能画到 sharedCanvas,主域用 `SubContextView` 组件显示。托管数据 key:`max_level`(最高关卡)、`max_troop`(单局最大兵力),用 `wxgame.score` 格式写(spec §2)。

任务(按 spec §8 的 R01~R07):
1. 通关刷新纪录时主域 `wx.setUserCloudStorage` 写两个 key。
2. 子域工程 + `SubContextView` 显示;子域 `wx.getFriendCloudStorage([board])` 拉好友榜 → 排序 → 绘制(头像/昵称/分数)→ **高亮自己** + 顶部"击败 N 位好友"。
3. 结算页弹条:"本局击败了 X 位好友"。
4. **定向挑战 PK(核心,spec §4.2)**:通关后「挑战好友」→ `wx.shareAppMessage({query:'act=challenge&lv=10&score=500&from=昵称'})`;好友从卡片进入 → `wx.getLaunchOptionsSync()/onShow` 解析 query → 直接进该关、HUD 显示"目标:超过好友的 500" → 结算本地对比胜负 → 提供「回敬挑战」。
5. 埋点:`rank_open/challenge_send/challenge_accept/challenge_result`(spec §6)。

红线:好友数据读取与绘制**必须在子域**(主域读不到,别绕);复杂点击交互(点好友发起挑战)由主域 UI 承接,子域只绘制;托管数据前端可改的作弊风险本阶段接受(spec §7),**不要为此加服务器**;不做全服榜(需服务器,后置)。
验收(主控核):① 真机好友榜可见、排序正确、自己高亮;② 结算页"击败 X 位好友"数字对;③ **挑战全链路**:A 分享 → B 点进直接落到该关且显示目标分 → B 结算显示胜负 → 可回敬;④ 四个埋点事件齐。
```

## T11 签到/每日任务/分享裂变

```text
项目 /Users/mm/project/蒜鸟的战斗,Cocos 工程 `cocos/`。前置:T8。新增 cocos/assets/scripts/systems/{Sign,DailyTask,Share}.ts + 对应 UI。

背景:**实现规范见 `签到任务分享系统设计.md`(权威)**。纯 IAA 阶段即可做(不涉及内购)。复用:T3 的 Progress 存档、T9 的 AdManager(补签/看广告任务)、T8 的 Analytics。

任务:
1. 每日签到(§1):7 天循环、断签重置、**补签看激励视频**;数据 `{lastSignDate, streak, claimedToday}`;奖励配置化。
2. 每日任务(§2):每日 3–5 个,**用事件总线订阅玩法埋点驱动进度**(level_win/gate_pick/ad_reward/share_click),不侵入玩法代码;全部完成给额外宝箱;跨日刷新。
3. 分享(§3):四个场景(晒战绩/挑战好友(与 T10 复用)/每日分享任务);**微信已取消分享成功回调**——奖励按"点击分享即发"或"每日首次分享发",靠**每日限次**防刷(spec §3.2 明确写了,别试图判断分享是否成功)。
4. UI:主界面签到入口(带红点)、任务面板(进度/领取/全清宝箱)、分享按钮。
5. 埋点:`sign_claim/task_progress/task_claim/share_click`。

红线:所有奖励每日限次;不做"好友助力"类深度裂变(需服务器,spec 已标后置);金币发放走 T3 的 Progress 接口不自己改存档;跨日判定用本地日期(风险已知并接受,spec §5)。
验收(主控核):① 签到 7 天/断签/补签(看广告)三条路径正确;② 每日任务进度随实际玩法推进、跨日刷新、全清给宝箱;③ 分享奖励每日限次生效(连点不重复发);④ 红点逻辑正确(有可领时亮);⑤ 埋点齐。
```

## T12 上线准备(主控自留,不派发)

```text
主控 session 自己做。前置:T9 完成(广告能跑=可验证商业模型)。

清单:
1. 资质与备案:微信小游戏类目审核、小程序备案(工信部)、隐私政策与《用户隐私保护指引》、防沉迷实名接入。
2. 流量主开通 + 正式 adUnitId 替换(T9 留了配置位)。
3. 名称查重与过审(「蒜鸟冲冲冲」,备候选名)。
4. 上线前自检:首包 ≤4MB、真机多机型跑通、切后台/断网/杀进程回归、广告未看完不发奖复测。
5. 冷启动素材:用 art_delivery 的品牌资源 + 录屏做买量素材(满屏蒜鸟冲锋是核心卖点)。
6. 数据看板:埋点接第三方或微信数据助手,盯 PRD §12 的验收指标(次留 40%/7 留 15%/IPU≥4/激励渗透 50%)。
7. 版号:按 Plan B——**先跑广告版验证**,数据达标再启动软著/版号/文网文(PRD §10.2)。
8. memory 更新:上线结论与真实数据回填。
```
